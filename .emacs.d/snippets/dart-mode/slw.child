# -*- mode: snippet -*-
# name: slw.child
# key: slw.child
# --
class $1 extends StatelessWidget {
  const $1({Key? key, required this.child}) : super(key: key);

  final Widget child;

  @override
  Widget build(BuildContext context) {
      return $2;
  }
}